ARG NODEJS_RUST_UBUNTU_VERSIONS

FROM ${NODEJS_RUST_UBUNTU_VERSIONS}

ARG CONFIG_DIR=/root/base

RUN mkdir -p ${CONFIG_DIR}
COPY ./bin/ ${CONFIG_DIR}
WORKDIR ${CONFIG_DIR}

RUN bash -c 'mv ${CONFIG_DIR}/* /usr/sbin'
