ARG RUST_UBUNTU_VERSIONS=rust.v1.90.0-ubuntu.v25.04

FROM ${RUST_UBUNTU_VERSIONS}

ARG CONFIG_DIR=/root/base

RUN mkdir -p ${CONFIG_DIR}
COPY ./nodejs.sh ${CONFIG_DIR}
WORKDIR ${CONFIG_DIR}

EXPOSE 4434

CMD ["bash","nodejs.sh"] 
